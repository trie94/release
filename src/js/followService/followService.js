
import * as firebase from "firebase/app"

const db = firebase.database()

export default class FollowService {

    getFollowers(userId) {
    }

    getFollowing(userId) {
    }

    follow() {
    }

    unfollow() {
    }
}